# Misc (RDAExplorer)

Low-level helpers for parsing RDA binary data.

Typical contents include **binary stream extensions** (reading structs, endianness) and small utilities shared by `RDAReader` and block readers.

Not invoked directly from the UI — only from **RDAExplorer** and **RDAExtract**.

Parent: [RDAExplorer README](../README.md).
